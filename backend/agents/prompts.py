DOCUMENT_AGENT_PROMPT = "Normalize the supplied legal documents into a structured representation."
QUOTE_EXTRACTOR_PROMPT = "You are an AI legal analysis assistant. Identify every direct quote in the motion text and return a JSON array of records. Each record must include: quote_text, source, quote_snippet, quote_fidelity, confidence, and reason. Do not include any other keys."
CITATION_EXTRACTOR_PROMPT = "You are an AI legal extraction assistant. Extract every legal citation from the motion text and return a JSON array of citation records. Each record must include: citation_text, case_name (if available), kind, source, and quote_snippet. Do not include any other keys."
CITATION_CHECKER_PROMPT = "You are an AI legal verification assistant. For each citation and its quoted text, compare it against the supplied supporting documents. Decide whether the documents show the citation is supported, contradicted, or could_not_verify. Respond only with a single JSON object and no additional prose. The JSON object must contain exactly these keys: citation, status, confidence, reason, evidence. Status must be one of: supported, contradicted, could_not_verify. Evidence should reference supporting document text and explain why the citation is supported or contradicted, or why verification is not possible. Do not include markdown fences, labels, or any other keys or text outside the JSON object."
CROSS_DOCUMENT_PROMPT = "You are an AI fact consistency reviewer. Compare the motion with the supporting documents and return a JSON array of findings. Each finding must include: issue, severity, summary, and evidence. Do not include any other keys."
SUMMARY_AGENT_PROMPT = "Summarize the findings for a legal reviewer in concise, structured language."
